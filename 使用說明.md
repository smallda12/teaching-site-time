# 時間的運算｜教學網 使用說明

數學 115學年度 第1學期 第1單元（第1～4 週，115.08.31 – 115.09.25）

**雙擊 `index.html` 就能用**，不需要安裝、不需要網路（只有「🔗 延伸學習」那一區要連網）。

---

## 兩種版本

| 開啟方式 | 版本 | 差別 |
|---|---|---|
| 雙擊本機的 `index.html` | **教室版**（右上黃標） | 點名可以上傳學生照片 |
| 用線上網址開啟 | **公開版**（右上藍標） | **照片上傳自動關閉** |

🔒 學生姓名與照片只存在**那一台電腦的瀏覽器**裡（localStorage／IndexedDB），
不會寫進任何檔案、也不會上傳到網路。換電腦可用點名區的「匯出備份檔」帶著走。

---

## 七個分區

| 分區 | 內容 |
|---|---|
| 🙋 點點名 | 學生照片＋姓名抽籤。**一輪每人只點一次**，全班點完會停 |
| 🃏 圖詞輪播 | 10 張學習圖卡，標題與說明各自是一顆發音鈕 |
| 🎬 教學影片 | 主教學影片（3 分 49 秒）＋右側播放清單，可單獨看四支主題短片 |
| 📚 教材教學 | 四個重點全部開放、不必照順序；流程是教學頁 → 形成性評量 |
| 📝 評量區 | 9 題總評量＋各重點正確率；**答錯不重答**，標出正解直接下一題 |
| 🧠 自學區 | 圖卡點擊放大，可自動輪播全部圖卡 |
| 🔗 延伸學習 | 均一教育平台的 6 支相關影片（**需要網路**，會開新分頁到 YouTube） |
| 側邊工具 | 🖊️ 畫筆 12 色｜⏱️ 三個各自獨立的計時器 |

---

## 四個教學重點

| # | 重點 | 教學頁 | 題數 | 主題短片 |
|---|---|---|---|---|
| 1 | ⏱️ 秒、分、時與換算 | P04–P08 | 2 題 | 43 秒 |
| 2 | ✖️ 時間乘法 | P13–P15 | 2 題 | 23 秒 |
| 3 | ➗ 時間除法 | P20–P22 | 2 題 | 24 秒 |
| 4 | 🕗 開始、經過與結束 | P27–P30 | 3 題 | 31 秒 |

開場 P01–P03、生活應用與總結 P37–P44、學習圖卡 P45–P54。

**評量正解序列**：`B A B B A A B A B`（A 4 題／B 5 題，沒有連續三題相同、也不是 ABAB 規律）。
選項位置與簡報頁面上的 A、B **完全一致**，網站不會隨機打亂。

---

## 老師常用的兩個開關

- **🔊 自動朗讀**（教材教學區）：關掉之後**切換頁面不會自己出聲**，方便老師自己講解；
  但主動按「🔊 再唸一次」仍然會播。設定會記住。
- **只播標題**（自學區）：自動輪播時只唸詞、不唸說明句，適合快速複習。

---

## 更新流程

1. 改教材 → 跑 `工具腳本\建置素材_圖片壓縮.py`
2. 改題目或口播稿 → 改 `教學網站\js\data.js`，再跑 `工具腳本\建置語音_全部.py`
   （語音清單是**從 `data.js` 自動讀出來的**，不要另外抄一份題庫）
3. **跳版本號**（不跳的話瀏覽器會繼續用快取，看起來像沒更新）：
   - 改程式或樣式 → `index.html` 底部 5 個 `?v=` 各加 1
   - 重產語音 → `js/config.js` 的 `語音.版本` 加 1
   - 換 PDF／影片／頁圖／圖卡 → `js/config.js` 的 `資源版本` 加 1
4. `git add -A && git commit && git push`，1–2 分鐘後線上版自動更新

## 每學期開學前

```bash
python 工具腳本/檢查延伸影片.py
```

外部影片被下架時網站不會有任何徵兆，要靠這支腳本檢查。
